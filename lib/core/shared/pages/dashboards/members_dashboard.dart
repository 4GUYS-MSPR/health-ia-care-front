import 'package:flutter/material.dart';

class MembersDashboard extends StatelessWidget {
  const MembersDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Membres — à venir'),
    );
  }
}