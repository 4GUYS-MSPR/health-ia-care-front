import 'package:flutter/material.dart';

class ExercisesDashboard extends StatelessWidget {
  const ExercisesDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exercices — à venir'),
    );
  }
}