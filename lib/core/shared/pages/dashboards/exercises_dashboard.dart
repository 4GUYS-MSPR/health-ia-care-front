import 'package:flutter/material.dart';

class ExercicesDashboard extends StatelessWidget {
  const ExercicesDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exercices — à venir'),
    );
  }
}